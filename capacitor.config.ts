import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.karvfan.app',
  appName: 'Karvfan',
  webDir: 'www',
  android: {
    path: 'karvfan-android'
  }
};

export default config;
